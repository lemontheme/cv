preview:
	convert -thumbnail x700 -density 300 -background white -alpha remove -quality 99 -colorspace RGB cv.pdf cv.png
	# convert -thumbnail x700 -density 300 -background white -alpha remove -quality 99 -colorspace RGB cv.pdf cv.png
